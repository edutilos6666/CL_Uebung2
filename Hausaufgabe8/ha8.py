'''

Schreiben Sie eine Klasse "konverter" in einem Modul konverter.py

Wenn diese von einem anderen Programm importiert wird, meldet sie sich mit einer Ausgabe auf dem Bildschirm an.

Danach sollen vom aufrufenden Programm folgende Methoden aufgerufen werden können:

int_zu_rom(integer-zahl) gibt eine integerzahl in römischer Notation zurück

int_zu_hex(integer-zahl) gibt eine Integerzahl als Hexadezimalzahl zurück

rom_zu_int(rom-string) gibt einen String, der eine römische Zahl darstellt, als Integerzahl zurück

hex_zu_int(hex-string) gibt einen String, der eine Hexadezimalzahl darstellt, als Integerzahl zurück

Alle Berechnungen während eines Programmdurchlaufs sollen außerdem in einer Liste des Moduls namens geschichte gespeichert werden. Am Ende des Hauptprogramms wird diese Liste aufgerufen und ausgegeben. 


- Kommentieren Sie alle Funktionen und Ihren Code
- Denken Sie daran, keine noch nicht besprochenen Funktionen/Module zu verwenden
- Geben Sie aussschließlich die Modul-Datei konverter.py ab (keine .zip-Datei!)




'''