# -*- coding: utf-8 -*-'''Hausaufgabe 1NAME1 MATRIKELNUMMER: Nijat Aghayev 108015250476NAME2 MATRIKELNUMMER: ...NAME3 MATRIKELNUMMER: ...Zwei allgemeine Hinweise, die fuer alle HAs gelten:Hinweis: Sie sollen grundsaetzlich (d.h. bei allen HAs) fuer IhreLoesung ausschliesslich Datenstrukturen und Operatoren verwenden, diewir schon im Kurs besprochen haben. Sie koennen _zusaetzlich_ am EndeIhres Programmes eine Loesung anfuegen, in denen Sie auchfortgeschrittene Python-Konstrukte verwenden.Hinweis: Die Aufgaben sind oft bewusst so gestellt, dass man sichGedanken dazu machen soll, wie man sie _sinnvoll_ loest. D.h. es gibtmeist etwas Spielraum, wie die Aufgaben zu interpretieren sind. Esgibt aber (fast) immer nur eine sinnvolle Interpretation, wie dieAufgabe zu loesen ist. Gegebenenfalls kommentieren Sie Ihre Loesungentsprechend, wenn Sie z.B. verschiedene (sinnvolle)Interpretationsmoeglichkeiten sehen, und entscheiden Sie sich fuerIhre Implementation fuer eine davon.'''# aktuell noch nicht benoetigt:#import sys#import retext1 = """Daß ich es nicht lassen kann, bei offenem Fenster zuschlafen. Elektrische Bahnen rasen läutend durch meineStube. Automobile gehen über mich hin. Eine Tür fällt zu. Irgendwoklirrt eine Scheibe herunter, ich höre ihre großen Scherben lachen,die kleinen Splitter kichern. Dann plötzlich dumpfer, eingeschlossenerLärm von der anderen Seite, innen im Hause. Jemand steigt dieTreppe. Kommt, kommt unaufhörlich. Ist da, ist lange da, gehtvorbei. Und wieder die Straße. Ein Mädchen kreischt: Ah tais-toi, jene veux plus. Die Elektrische rennt ganz erregt heran, darüber fort,fort über alles. Jemand ruft. Leute laufen, überholen sich. Ein Hundbellt. Was für eine Erleichterung: ein Hund. Gegen Morgen kräht sogarein Hahn, und das ist Wohltun ohne Grenzen. Dann schlafe ich plötzlichein."""text2 = """Fast genau 24 Stunden nach dem Stromausfall vom Mittwoch,der die gesamte Ruhr-Uni lahmgelegt hatte, atmeten auch dieMitarbeiter der Pressestelle der Ruhr-Universität auf. 'Am Freitagläuft wieder alles im Normalbetrieb', meldete Pressedezernent JensWylkop am Donnerstag um 14.50 Uhr. Der Strom fließt, die Studierendenam Freitag dann auch wieder. Sie hatten ab Mittwochnachmittag und dengesamten Donnerstag ungewollt stromfrei. Auf dem Campus fanden keineVeranstaltungen, keine Prüfungen statt. Die Uni-Bibliothek bliebebenso wie die Mensa und alle anderen Einrichtungen auf dem Campusgeschlossen. Das sorgte für ein ungewohntes Bild. Es war, als wäre einSonntag mitten in die Woche gefallen. Nur ganz vereinzelt verlorensich Menschen auf dem Campus. Im Jahr des 50. Geburtstags hatten siean der Ruhr-Uni so einige Feier-Ideen. Auf die, alle Lichterauszumachen, waren sie nicht gekommen. Ein Stromausfall passiertselten geplant und dann passiert eben lange nichts mehr."""# Autorenschaftzuschreibung: Eine typische CL-Aufgabe ist es, den# (unbekannten) Autor eines Textes zu bestimmen. Dazu werden# unterschiedliche Merkmale genutzt, z.B. die durchschnittliche# Wortlaenge, die durchschnittliche Satzlaenge oder auch bestimmte# charakteristische Verwendungen von Vokalen u.a. Im Folgenden sollen# zwei Texte verschiedener Autoren miteinander verglichen werden (in# den Variablen text1 und text2 gespeichert).# Aufgabe 1: Ermitteln Sie die durchschnittliche Wortlaenge der Texte# (in Anzahl der Buchstaben) und geben Sie sie aus. Ueberlegen Sie# dazu zunaechst, woran Sie einzelne Woerter erkennen koennen, und####### Loesungen ############### Ich denke , es ist unmöglich diese Aufgaben ohne for-Schleife zu loesen# Ich habe versucht, aber koennte nicht finden wie  man z.B. Woerte des Satzes identifizieren kann ,# und wie Anzahl der Woerter im Satz hochzaehlen kann .# Aufgabe 1def fn_durch_wort_laenge(text):    anzahl_woerter = 0    anzahl_buchstaben = 0    text = text.replace("\n", " ")    splitted = text.split(" ")    #anzahl_woerter = len(splitted)    for word in splitted:        if word == "": continue        ws = word.split(",")        for w in ws:            if w == "": continue            if w.startswith("'"): w = word[1:len(w)]            if w.endswith("'") or w.endswith(",") or w.endswith(".") or w.endswith(":"): w = w[0:len(                w) - 1]            # print(w)            anzahl_woerter += 1            anzahl_buchstaben += len(w)    #print("anzahl woerter = ", anzahl_woerter, anzahl_buchstaben)    durch_laenge = anzahl_buchstaben / anzahl_woerter    return durch_laengedurch_laenge = fn_durch_wort_laenge(text1)print("durchschnittliche Wortlaenge des text1 = ", durch_laenge)durch_laenge = fn_durch_wort_laenge(text2)print("durchschnittliche Wortlaenge des text2 = ", durch_laenge)# Aufgabe 2: Ermitteln Sie die durchschnittliche Satzlänge des Textes# (in Anzahl der Woerter) und geben Sie sie aus. Nehmen Sie dazu# vereinfachend an, dass jeder Punkt (".") ein Satzende markiert.# Aufgabe 2def fn_anzahl_woerter(line):    splitted = line.split(" ")    anzahl = 0    for word in splitted:        if word == "": continue        anzahl += 1    return anzahldef fn_durch_satz_laenge(text):    lines = text.split(".")    anzahl_saetze = 0    anzahl_woerter = 0    for line in lines:        if line == "": continue        line = line.replace("\n", " ").strip()        anzahl_saetze += 1        anzahl_woerter += fn_anzahl_woerter(line)        #print(line)    # print("Statistik: ", anzahl_saetze, anzahl_woerter)    durch_laenge =  anzahl_woerter / anzahl_saetze    return durch_laengedurch_laenge = fn_durch_satz_laenge(text1)print("durchschnittliche Satzlaenge des text1 = ", durch_laenge)durch_laenge = fn_durch_satz_laenge(text2)print("durchschnittliche Satzlaenge des text2 = ", durch_laenge)######### Loesungen mit fortgeschrittenen Konzepten ###############import reprint("*** Forgeschritten ***")# Aufgabe 1def fn_durch_wort_laenge_2(text):    anzahl_woerter = 0    anzahl_buchstaben = 0    pattern = "\s+|\,\s+|\.\s+|\:\s+|\.$"    words = re.split(pattern, text)    for word in words:        if word == "": continue        if (word.startswith("'")):            word = word[1:len(word)]        if (word.endswith("'")):            word = word[0:len(word) - 1]        #print(word)        anzahl_woerter += 1        anzahl_buchstaben += len(word)    #print("anzahl woerter = ", anzahl_woerter, anzahl_buchstaben)    durch_laenge = anzahl_buchstaben / anzahl_woerter    return durch_laenge# setzen Sie Ihre Loesung dann entsprechend um.durch_laenge = fn_durch_wort_laenge_2(text1)print("durchschnittliche Wortlaenge des text1 = ", durch_laenge)durch_laenge = fn_durch_wort_laenge_2(text2)print("durchschnittliche Wortlaenge des text2 = ", durch_laenge)# Aufgabe 2: Ermitteln Sie die durchschnittliche Satzlänge des Textes# (in Anzahl der Woerter) und geben Sie sie aus. Nehmen Sie dazu# vereinfachend an, dass jeder Punkt (".") ein Satzende markiert.#Aufgabe 2def fn_anzahl_woerter_2(line):    pattern = "\s+|\,\s+|\:\s+"    splitted = re.split(pattern, line)    anzahl = 0    for word in splitted:        if word == "": continue        anzahl += 1    return anzahl# hier sie haben geschrieben , dass jeder Punkt ein Satzende markiert , dementsprechend# ... 14.50 Uhr.  wird in zwei Saetze aufgeteilt , das ist nicht mein Fehler , ich versuche# mir ihren Hinweisen nachzugehen.def fn_durch_satz_laenge_2(text):    anzahl_saetze = 0    anzahl_woerter = 0    pattern = "\."    splitted = re.split(pattern , text)    for line in splitted:        #print(line)        if line == "": continue        anzahl_saetze +=1        anzahl_woerter += fn_anzahl_woerter_2(line)    # print("Statistik: ", anzahl_saetze, anzahl_woerter)    durch_laenge = anzahl_woerter / anzahl_saetze    return durch_laengedurch_laenge = fn_durch_satz_laenge_2(text1)print("durchschnittliche Satzlaenge des text1 = ", durch_laenge)durch_laenge = fn_durch_satz_laenge_2(text2)print("durchschnittliche Satzlaenge des text2 = ", durch_laenge)